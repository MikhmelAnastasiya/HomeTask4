pipeline {
  agent { docker { image 'python:3.8.2' } }
  stages {
  stage('build') {
    
      steps {
        sh 'git clone https://github.com/MikhmelAnastasiya/HomeTask4'
      }
    }
  stage('builkk') {
    
      steps {
        sh 'cd C:/Users/Anastasiya_Mikhmel/Documents/DQE Training/Module 4/Task/HomeTaskModule4Mikhmel/TestFramework'
      }
    }
    stage('test') {
    
      steps {
        sh 'python main.py smoke'
      }
    }
  }
}