pipeline {
  agent { docker { image 'python:3.8.2' } }
  stages {
    stage('build') {
      steps {
        sh 'CMD [ "python", "/TestFramework/main.py" ]'
      }
    }
    stage('test') {
      steps {
        sh 'python main.py smoke'
      }
    }
  }
}