pipeline {
  agent { docker { image 'python:3.7.2' } }
  stages {
    stage('build') {
      steps {
        sh 'pip install -r requirements.txt'
      }
    }
    stage('test') {
      steps {
        sh 'cd "C:\Users\Anastasiya_Mikhmel\Documents\DQE Training\Module 4\Task\HomeTaskModule4Mikhmel\TestFramework"
python main.py smoke'
      }
    }
  }
}